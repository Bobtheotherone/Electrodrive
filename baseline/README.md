# Phase 0 baseline artifacts

This directory is populated by `scripts/phase0_baseline.py` and is intended
to hold environment snapshots, metrics, and logs for:

- BEM probe
- EDE plane_point solve
- FMM sanity / stress / parallelism tests

It is safe to delete and regenerate. Commit or ignore these artifacts
according to your workflow preferences.
